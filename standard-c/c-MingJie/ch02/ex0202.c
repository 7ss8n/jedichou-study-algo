/* MingJie C - ex0202.c */
#include <stdio.h>

int main(void)
{
	int na, nb;

	puts("Pls input two integers");
	printf("na: "); scanf("%d", &na);
	printf("nb: "); scanf("%d", &nb);

	printf("na+nb=%d, na*nb=%d\n", na+nb, na*nb);

	return (0);
}
