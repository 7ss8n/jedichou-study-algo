/* define and echo two integer variables */
#include <stdio.h>
int main(void)
{
	int vx, vy;
	vx = 57;
	vy = vx + 10;

	printf("vx value: %d.\n", vx);
	printf("vy value: %d.\n", vy);

	return (0);
}
