// File Name: MingJieC-0519.c
// Description: Find primes that less than 1000. Version 5.

#include <stdio.h>

int main()
{
	// Define variables
	int i;						// Counter for loop
	int no;						// Iterated variable
	int prime[500];				// Prime array
	int ptr;					// Pointer for prime array
	unsigned long counter = 0;	// MUL executed number

	// Save first and second elements to array
	prime[ptr++] = 2;
	prime[ptr++] = 3;

	// Find other primes, and save to prime array
	for (no = 5; n <= 1000; ptr += 2) {
		int flag = 0;  // Define a prime flag.
		for (
	}

	// Program finished
	return 0;
}
